---
import Navitem from "../shared/Navitem.astro";

const navItems = [
  {
    href: "#uvod",
    text: "Úvod",
  },
  {
    href: "#aktuality",
    text: "Aktuality",
  },
  {
    href: "#servis",
    text: "Servis",
  },
  {
    href: "#kontakt",
    text: "Kontakt",
  },
  {
    href: "#o-nas",
    text: "O nás",
  },
  {
    href: "#znacky",
    text: "Značky",
  },
];
---

<header class="bg-white sticky top-0 z-40"> 
  <div class="container mx-auto px-4 flex items-center justify-between  py-4">
    <a href="#" class="flex items-center">
      <img src="/images/logo.png" alt="Logo Cyklo Vackar" class="h-16 w-auto" />
    </a>

    <nav class="flex items-center">
      {/* Desktop Navigation */}
      <ul class="hidden md:flex gap-6 text-heading-2">
        {navItems.map((item) => <Navitem {...item} />)}
      </ul>

      {/* Mobile Menu Button */}
      <button
        class="md:hidden text-gray-700 hover:text-primary focus:outline-none"
        onclick="toggleMobileMenu()"
      >
        <svg
          class="h-6 w-6"
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M4 6h16M4 12h16m-7 6h7"></path>
        </svg>
      </button>

      {/* Mobile Navigation */}
      <ul
        id="mobile-menu"
        class="hidden absolute top-full right-0 w-full md:w-auto bg-white p-4 space-y-2"
      >
        {
          navItems.map((item) => (
            <li class="text-center">
              <a
                href={item.href}
                class="duration-300 font-medium ease-linear hover:text-primary py-3 block"
              >
                {item.text}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
  <div id="top-info-banner" class="bg-white pb-2 sm:pb-2">
    <div class="container rounded-lg bg-slate-100 mx-auto px-3 flex flex-col sm:flex-row sm:items-center justify-between">
      {/* Text Content - Now centered on all screen sizes */}
      <div class="flex-1 text-center">  {/* Removed sm:text-left */}
        <p class="text-sm leading-6 text-gray-900 sm:text-base whitespace-nowrap overflow-hidden overflow-ellipsis">
          Jaro za rohem 🌷, tak nečekejte frontu 🚴‍♂️, přivezte kolo na servis už teď! 
        </p>
      </div>
  
      {/* Dismiss Button - Stays on the right */}
      <div class="flex sm:flex-none justify-center sm:justify-end mt-2 sm:mt-0">
        <button type="button" class="-m-3 p-3 focus-visible:outline-offset-[-4px]" onclick="dismissBanner()">
          <span class="sr-only">Dismiss</span>
          <svg class="h-5 w-5 text-gray-900" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
            <path d="M6.28 5.22a.75.75 0 00-1.06 1.06L8.94 10l-3.72 3.72a.75.75 0 101.06 1.06L10 11.06l3.72 3.72a.75.75 0 101.06-1.06L11.06 10l3.72-3.72a.75.75 0 00-1.06-1.06L10 8.94 6.28 5.22z" />
          </svg>
        </button>
      </div>
    </div>
  </div>
</header>

<script is:inline>
  function dismissBanner() {
    const banner = document.getElementById('top-info-banner'); 
    if (banner) {
      banner.style.display = 'none';
    }
  }
</script>

<script is:inline>
  function toggleMobileMenu() {
    const mobileMenu = document.getElementById("mobile-menu");
    if (mobileMenu) {
      mobileMenu.classList.toggle("hidden");
    }
  }
</script>
